pwd = show the folder path I'm currently in
ls = display all the files and folders inside current directory
git init : initialize a new local git repository in the current folder
git status : show whether files are created, saves or not in git
git add : add specific files to the waiting area before being committed
git commit : record staged changes into repository history with a message

So usefull ! 